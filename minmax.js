/**
 * 
 * @param {Array} arr 
 */
let getMin = (arr) =>{
    console.log(min);
    for(let element of arr){
        if(element<min){
            min=element;
        }
    }
    return min;
}

console.log(getMin([3,1,5,6,2]));

let getMin = (arr) =>{
}